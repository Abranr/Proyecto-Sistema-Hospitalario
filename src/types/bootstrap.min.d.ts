declare module '../assets/js/bootstrap.min.js' {
  // Puedes especificar los tipos de las funciones y objetos que exporta
  // Por ejemplo, si Bootstrap tiene una función `alert`, puedes declararla así:
  // export function alert(message: string): void;

  // Usa `any` si no estás seguro de los tipos específicos
  const bootstrap: any;
  export default bootstrap;
}